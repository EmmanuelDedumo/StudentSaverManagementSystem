document.querySelector('.add-expenses').addEventListener('click', () => {
    // Functionality to add expenses
});

document.querySelector('.manage-expenses').addEventListener('click', () => {
    // Functionality to manage expenses
});

document.querySelector('.user-profile').addEventListener('click', () => {
    // Functionality to view user profile
});
